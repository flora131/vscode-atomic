(no external research applicable)

The 10 files in `src/typings/` are all pure ambient TypeScript declaration files (`.d.ts`). Their sole purpose is to patch the TypeScript compiler's view of the global environment or to work around tooling quirks — none of them pull in any external library whose documentation would be meaningful to fetch for a Tauri/Rust porting analysis. Concretely:

- `electron-cross-app-ipc.d.ts` declares types for a **custom, internal** Electron module (`Electron.CrossAppIPC`). There is no public npm package or third-party docs to fetch; the types are defined in-file and are relevant only because they expose how VS Code's Electron build implements secure inter-process communication via code-signature-verified channels (Mach ports on macOS, named pipes on Windows). A Tauri port would need to replace this with Tauri's `tauri::ipc` or `tauri-plugin-ipc`, but that analysis belongs to a Tauri-specific research partition, not here.
- `editContext.d.ts` re-declares the [W3C EditContext API](https://www.w3.org/TR/edit-context/) for TypeScript, filling a gap in `lib.dom.d.ts`. This is a browser-native Web API, not a third-party library. Tauri's WebView renders in the OS's native browser engine, so the EditContext API would simply be available (or not) depending on the WebView version — no external docs are needed to understand the declaration file itself.
- `base-common.d.ts` declares `requestIdleCallback`/`cancelIdleCallback` and opaque `Timeout` types for cross-context compatibility. These are standard Web/Node APIs; no library docs required.
- `vscode-globals-nls.d.ts`, `vscode-globals-product.d.ts`, `vscode-globals-ttp.d.ts` declare VS Code-internal global variables (`_VSCODE_NLS_MESSAGES`, `_VSCODE_FILE_ROOT`, `_VSCODE_WEB_PACKAGE_TTP`, etc.). These are runtime injection points specific to the VS Code build system, not external packages.
- `thenable.d.ts` declares the `Thenable<T>` interface (a `PromiseLike<T>` alias). Self-contained, no external dependency.
- `css.d.ts` declares `declare module "vs/css!*"` and `declare module "*.css"` so TypeScript accepts CSS imports. Self-contained module augmentation.
- `crypto.d.ts` is a partial hand-copy of the standard `lib.dom.d.ts` `SubtleCrypto`/`Crypto` interfaces for use in `common/` layers that do not depend on the full DOM. It references the MDN Web Crypto API but is itself a standalone subset — no library to fetch docs for.
- `copilot-api.d.ts` provides a narrow ambient shim for `@vscode/copilot-api` because that package's own `.d.ts` files use extensionless relative imports incompatible with `moduleResolution: "nodenext"`. The shim exposes `CAPIClient`, model types, and request helpers. This is the closest thing to an external library in the partition, but the declaration file already contains all the type information needed; the upstream package is an internal Microsoft package not publicly documented in a way that would illuminate a Tauri porting decision.

In summary, `src/typings/` is entirely a set of thin ambient shims that smooth over TypeScript tooling gaps and expose internal VS Code global conventions. None of these files introduces an external framework or library with authoritative documentation that would be central to understanding what it takes to port VS Code to Tauri/Rust. The Electron-specific type (`CrossAppIPC`) is the only one with direct porting relevance, but it documents a bespoke internal module rather than a public library. No external research is warranted for this partition.
