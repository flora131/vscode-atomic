(no external research applicable)

`src/bootstrap-meta.ts` is a 55-line, self-contained early-stage loader whose entire job is to resolve `product.json` and `package.json` at process start — either from build-time string-patch markers or from the filesystem via `require`. It imports only two TypeScript interfaces (`IProductConfiguration`, `INodeProcess`) for type-checking purposes and uses only Node.js built-ins (`createRequire`, `process.env`, `process.isEmbeddedApp`). There are no third-party libraries involved, so no external library documentation is relevant. When porting to Tauri/Rust the analogous pattern is embedding metadata at compile time (e.g. via `include_str!`, `env!`, or Cargo's `build.rs` writing constants), and at runtime reading Tauri's `tauri.conf.json`-derived values through `tauri::Config` or the `ProductInfo` struct — but these are architectural decisions, not documentation that must be researched before understanding or translating this specific file, whose logic is entirely transparent from the source alone.
