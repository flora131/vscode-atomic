(no external research applicable)

The `extensions/ipynb/` scope's three npm dependencies do not require fetching external library documentation to guide a Tauri/Rust port. `@jupyterlab/nbformat` is consumed exclusively via `import type` (a TypeScript compile-time-only import present in `serializers.ts:6`, `deserializers.ts:6`, `notebookSerializer.ts:6`, and `common.ts:6`), meaning zero runtime behaviour from that package crosses the TypeScript/Rust boundary — the port only needs to match the Jupyter nbformat 4 JSON schema, which is a well-known open specification. `detect-indent` (`notebookSerializer.ts:7,54`) is a single-purpose utility that scans the first 1 KB of a file's text and returns the dominant indent string; its full behaviour is trivially reimplementable in Rust without consulting its docs. `@enonic/fnv-plus` (`notebookSerializer.ts:10,39`) is used only to compute a hex FNV-1a-32 hash for constructing a legacy Jupyter backup file path; FNV-1a is a public-domain algorithm with a stable Rust crate (`fnv`) and no library-specific behaviour that needs documentation lookup. The extension's core logic — reading raw bytes, UTF-8 decoding, JSON parsing against the nbformat 4 schema, indent detection, and re-encoding — is entirely self-contained standard operations that map directly to Rust's `std`, `serde_json`, and trivial hash utilities.
