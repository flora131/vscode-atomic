(no external research applicable)

The `extensions/simple-browser/` scope (10 files, 636 LOC) is a webview panel serializer that uses only the VS Code extension API and standard browser DOM APIs. Inspection of the extension shows no non-trivial third-party library dependencies, so there are no external documentation sources that are central to assessing what it would take to port this component to Tauri/Rust. Skipping external research is the correct outcome for this partition.
