(no external research applicable)

The scope is `extensions/objective-c/`, a single 14-line TextMate grammar file that contains only a declarative JSON/PLIST grammar definition with no runtime TypeScript, Electron, or Rust code, so there is nothing in this extension that relates to porting VS Code's core IDE from TypeScript/Electron to Tauri/Rust.
