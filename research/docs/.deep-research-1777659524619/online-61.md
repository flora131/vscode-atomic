(no external research applicable)

The `extensions/html/` partition (61 LOC) contains only TextMate grammar files, JSON snippet definitions, and language-configuration JSON — all static data assets with no runtime library dependencies — so no external library documentation is central to porting this partition to Tauri/Rust.
