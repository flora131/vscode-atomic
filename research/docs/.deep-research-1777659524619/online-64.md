# Partition 64: `extensions/razor/` — Online Research

(no external research applicable)

The `extensions/razor/` directory is a pure declarative extension consisting solely of a TextMate grammar file and a language-configuration JSON file (44 LOC total); it imports no runtime libraries, executes no TypeScript logic, and has no Electron or Node.js dependencies. Porting VS Code's core IDE to Tauri/Rust has no bearing on this partition — the files are static data assets that would be copied verbatim into any replacement host unchanged.
