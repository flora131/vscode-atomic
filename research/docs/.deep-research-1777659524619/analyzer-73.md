## Analysis: extensions/objective-c/ — Partition 73 of 79

### Files Analysed

| File | LOC | Role |
|------|-----|------|
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/build/update-grammars.js` | 14 | Build-time grammar downloader (the sole implementation file) |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/package.json` | 55 | Extension manifest; declares two language contributions and two grammar contributions |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/cgmanifest.json` | 30 | Component-governance manifest; pins two upstream grammar repos by commit hash |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/syntaxes/objective-c.tmLanguage.json` | (large) | TextMate grammar for `.m` files; downloaded artefact, not hand-authored |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/syntaxes/objective-c++.tmLanguage.json` | (large) | TextMate grammar for `.mm` files; downloaded artefact, not hand-authored |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/language-configuration.json` | (small) | Declarative bracket/comment/indent configuration |
| `/home/norinlavaee/projects/vscode-atomic/extensions/objective-c/package.nls.json` | (small) | NLS string table for display name and description |

---

### Per-File Notes

#### `build/update-grammars.js` (14 LOC) — the only implementation file

**Imports**

- Line 7: `var updateGrammar = require('vscode-grammar-updater');`
  - `vscode-grammar-updater` is an npm devDependency (not vendored in the repo); its `update()` function fetches a file from a GitHub repository at a given ref and writes it to a local path.

**Entry function**

- Lines 9–12: `async function updateGrammars()` — defines the async orchestration function.
- Line 10: first `await updateGrammar.update(...)` call:
  - Source repo: `'jeff-hykin/better-objcpp-syntax'`
  - Source path inside that repo: `'autogenerated/objcpp.tmLanguage.json'`
  - Destination: `'./syntaxes/objective-c++.tmLanguage.json'`
  - Branch: `'master'`
- Line 11: second `await updateGrammar.update(...)` call:
  - Source repo: `'jeff-hykin/better-objc-syntax'`
  - Source path: `'autogenerated/objc.tmLanguage.json'`
  - Destination: `'./syntaxes/objective-c.tmLanguage.json'`
  - Branch: `'master'`
- Line 14: `updateGrammars();` — top-level invocation; the script is run directly via `node ./build/update-grammars.js` (per `package.json:12`, the `update-grammar` npm script).

**Control flow**

The script is entirely linear: it sequentially downloads two TextMate grammar files from two different GitHub repositories (each call is awaited) and overwrites the local `.json` artefacts in `./syntaxes/`. There are no conditionals, no error handling blocks, and no configuration parsing. The function is invoked once at the bottom of the file and exits when both awaits resolve.

**Data flow**

```
GitHub (jeff-hykin/better-objcpp-syntax @ master)
  → autogenerated/objcpp.tmLanguage.json
    → ./syntaxes/objective-c++.tmLanguage.json   (written to disk)

GitHub (jeff-hykin/better-objc-syntax @ master)
  → autogenerated/objc.tmLanguage.json
    → ./syntaxes/objective-c.tmLanguage.json     (written to disk)
```

No data is returned; side effects are file writes only.

**Dependencies**

- `vscode-grammar-updater` (external npm package, not present in-repo): provides the `update(repoSlug, srcPath, destPath, transform, branch)` API used on lines 10–11.

#### `package.json`

- Lines 16–36: Registers two VS Code language identifiers — `objective-c` (`.m` extension) and `objective-cpp` (`.mm` extension) — each pointing to the shared `./language-configuration.json`.
- Lines 38–49: Contributes two TextMate grammars mapping language ids to scope names (`source.objc`, `source.objcpp`) and the corresponding downloaded `.tmLanguage.json` artefacts.
- Lines 11–13: The `update-grammar` script invokes the build script: `node ./build/update-grammars.js`.

#### `cgmanifest.json`

- Lines 8–10: Pins `jeff-hykin/better-objcpp-syntax` at commit `5a7eb15eee382dd5aa388bc04fdb60a0d2128e14`.
- Lines 19–21: Pins `jeff-hykin/better-objc-syntax` at commit `119b75fb1f4d3e8726fa62588e3b935e0b719294`.
- Used by Microsoft's component-governance tooling to track open-source provenance; has no runtime effect.

---

### Cross-Cutting Synthesis

This partition is a **pure TextMate grammar extension**. Its sole implementation file (`build/update-grammars.js`) is a 14-line build-time utility that downloads pre-generated grammar JSON from two upstream GitHub repositories and writes them to `./syntaxes/`. At runtime, VS Code's TextMate tokenization engine reads the two `.tmLanguage.json` artefacts directly; no TypeScript or Rust code from this extension is loaded. The extension contributes nothing beyond declarative JSON manifests and the two grammar files.

**Why this partition is non-essential to a Tauri/Rust port**

The `build/update-grammars.js` script is a development-time artefact maintenance tool. The runtime deliverables it produces — `syntaxes/objective-c.tmLanguage.json` and `syntaxes/objective-c++.tmLanguage.json` — are static JSON grammar files in TextMate format. Any editor capable of consuming TextMate grammars (including Tauri-based editors that integrate a grammar engine such as `tree-sitter` or `syntect`) can consume these files verbatim. The grammar files themselves require no TypeScript/Electron APIs, no VS Code extension host, and no Node.js runtime. Porting this partition to Tauri/Rust reduces to: (1) copying the two `.tmLanguage.json` files, and (2) replacing the `update-grammars.js` download script with an equivalent shell script, Makefile rule, or Rust build script — a trivial one-to-one substitution. No architectural decisions depend on this partition.

---

### Out-of-Partition References

- `vscode-grammar-updater` (npm, external): The npm package whose `update()` function is called at lines 10–11. It lives outside this repository entirely; its implementation fetches files from the GitHub raw content API.
- VS Code TextMate tokenization engine: consumes the grammar JSON at runtime; implemented in `src/vs/workbench/services/textMate/` (out of partition scope).
- Microsoft component-governance tooling: consumes `cgmanifest.json` (out of partition scope).
