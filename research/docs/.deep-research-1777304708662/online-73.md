(no external research applicable)

The assigned scope — `extensions/objective-c/` — consists exclusively of static TextMate grammar JSON files (`objective-c.tmLanguage.json`, `objective-c++.tmLanguage.json`) and a `language-configuration.json`. There is no TypeScript runtime code, no Electron API surface, no Node.js imports, and no Rust FFI boundary anywhere in this partition. The research question asks about porting VS Code's core IDE from TypeScript/Electron to Tauri/Rust, a concern that is entirely architectural and applies to the host shell and extension host process — not to grammar-only extensions that are consumed as pure data by whatever tokenizer engine the host exposes (VS Code's vscode-textmate today, or a Rust equivalent such as syntect tomorrow). Because this partition would be copied or adapted with no changes regardless of the underlying host technology, no external documentation about Tauri, wry, tao, syntect, or any Rust crate is relevant here.
