(no external research applicable)

The scope for partition 58 is `extensions/typescript-basics/`, which is a TextMate grammar and snippet-only contribution containing no runtime code. It holds only static JSON/YAML grammar definition files and snippet files that are bundled as-is into the extension. The build script (`build/update-grammars.js` or similar) uses `vscode-grammar-updater` solely to pull in updated upstream grammar files from the TypeScript repository. There is no TypeScript runtime logic, no Electron API usage, no Node.js service code, and no Tauri/Rust interop surface in this directory. TextMate grammars are consumed by the editor's tokenization engine regardless of the host framework (Electron or Tauri), and the grammar/snippet files themselves require no migration or porting work. No external documentation research is needed or applicable here.
