# Partition 45: extensions/debug-auto-launch/

(no external research applicable)

This partition consists of a single-file Node.js auto-attach helper that bridges VS Code's debug UI to the ms-vscode.js-debug extension using only the VS Code extension API and Node.js standard library modules (net, child_process); it has no third-party dependencies that would require any external research to evaluate porting considerations for a Tauri/Rust target.
