(no external research applicable)

The partition covers a single build-infrastructure file (`extensions/esbuild-extension-common.mts`, 102 LOC) that provides shared esbuild bundling configuration for VS Code extensions. It has no runtime IDE logic, no Tauri or Rust integration points, and no core IDE functionality; therefore, fetching external library or framework documentation is not central to answering the research question about porting VS Code's core IDE functionality to Tauri/Rust.
