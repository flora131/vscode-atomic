(no external research applicable)

The scoped partition (`extensions/html/`) contains only declarative metadata files — `package.json`, `language-configuration.json`, a TextMate grammar, and snippets — with zero TypeScript source code and no external library dependencies, so there are no libraries or APIs to research in the context of porting VS Code from TypeScript/Electron to Tauri/Rust.
