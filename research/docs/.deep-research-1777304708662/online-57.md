(no external research applicable)

`src/bootstrap-import.ts` is a 101-line Node.js custom ESM loader hook (using only `node:url`, `node:fs`, and `node:path` built-ins) that redirects bare package specifiers to absolute file URLs at dev-time by reading `package.json` dependency lists and resolving paths under a local `node_modules` tree. It exports the two standard Node.js loader lifecycle functions `initialize` and `resolve` as defined by the Node.js loader API (`https://nodejs.org/docs/latest/api/module.html#initialize`). No external npm packages are imported; the entire file depends solely on Node.js built-in modules. In the context of porting VS Code to Tauri/Rust, this file is a pure Node.js/Electron build-infrastructure shim whose entire purpose is eliminated: Tauri uses neither Node.js module resolution nor ESM loader hooks. There is no external library or framework documentation that is central to understanding this file for porting purposes — the relevant knowledge is entirely contained in the Node.js built-in API, which itself becomes irrelevant on the Tauri target.
