(no external research applicable)

The single file in partition 66, `src/server-cli.ts`, is a thin Node.js bootstrapper (~30 LOC) that sets up global state, loads NLS configuration, resolves dev paths, initialises ESM support, and delegates execution to `vs/server/node/server.cli.js`. Its behaviour is entirely self-contained within the VS Code codebase and relies only on standard Node.js built-in modules and sibling bootstrap helpers — none of which require fetching external library or framework documentation to understand in the context of a Tauri/Rust porting analysis. The porting implications of this file are derivable directly from reading the source: the equivalent in a Tauri/Rust port would be the Rust binary entry point (`main.rs`) that parses CLI arguments and dispatches to the appropriate server command, replacing the Node.js-specific bootstrapping (NLS, ESM loader, process globals) with Rust standard-library and Tauri CLI primitives — no external documentation fetch is needed to establish that conclusion.
