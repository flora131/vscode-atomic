(no external research applicable)

The file `src/bootstrap-fork.ts` relies exclusively on Node.js built-in APIs — `process.on()`, `process.send()`, `process.exit()`, `process.env`, and dynamic `import()` — with no third-party npm packages or external frameworks involved. Because the locator findings confirm there are no external libraries central to this file, fetching external documentation would not contribute meaningful findings for the Tauri/Rust porting research question scoped to this partition.
