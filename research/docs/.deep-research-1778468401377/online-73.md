(no external research applicable)

The `.devcontainer/` configuration — which sets up a TypeScript/Node base image with desktop-lite, Rust, and noVNC/VNC forwarding — is purely build-time scaffolding for the developer environment and has no bearing on the runtime architecture question of porting VS Code's core IDE functionality from TypeScript/Electron to Tauri/Rust. External documentation about dev container specs, noVNC, or Rust toolchain installation in containers would not inform decisions about which VS Code subsystems to rewrite, how to map Electron IPC to Tauri commands, or how to bridge the TypeScript extension host to a Rust backend. The `.devcontainer/` scope is a convenience wrapper that happens to provision Rust tooling, but it neither constrains nor guides the porting work itself.
