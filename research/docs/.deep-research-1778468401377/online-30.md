(no external research applicable) The `extensions/references-view/package.json` lists only `@types/node` under `devDependencies` and no `dependencies` at all, confirming this extension relies exclusively on the built-in `vscode.d.ts` API (TreeView, language providers such as references, call hierarchy, and type hierarchy) with no third-party runtime libraries that would require porting consideration for a Tauri/Rust target.
