(no external research applicable)

The `extensions/grunt/` directory (2 files, 382 LOC) declares zero runtime dependencies in its `package.json` — `"dependencies": {}`. The only `devDependency` is `@types/node` at version `22.x`, which is a TypeScript type definition package used exclusively at compile time and requires no external documentation for a porting effort. The single source file, `src/main.ts`, imports only three Node.js standard-library modules (`path`, `fs`, `child_process`) and the ambient `vscode` API namespace. None of these represent third-party libraries with external documentation that would be central to planning a Tauri/Rust port. The porting work for this scope is entirely self-contained: the Node.js APIs in question (`fs.exists`, `cp.exec`) have well-known Rust equivalents (`std::fs`, `std::process::Command`), and the VS Code extension surface (`TaskProvider`, `ShellExecution`, `FileSystemWatcher`, `OutputChannel`) is internal VS Code API that gets replaced wholesale by Tauri's command and event system. No external documentation fetch is warranted.
