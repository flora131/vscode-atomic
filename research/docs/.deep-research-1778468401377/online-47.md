(no external research applicable)

The `extensions/debug-server-ready/` scope (2 files, 411 LOC) is a self-contained VS Code extension whose only external dependency is the `vscode` extension host API itself and Node's built-in `crypto` and `util` modules. There are no third-party npm packages in `dependencies` or `devDependencies` beyond `@types/node` (a type-only dev dependency). Every capability the extension uses — `vscode.debug.onDidStartDebugSession`, `vscode.debug.onDidTerminateDebugSession`, `vscode.debug.registerDebugConfigurationProvider`, `vscode.debug.registerDebugAdapterTrackerFactory`, `vscode.debug.startDebugging`, `vscode.debug.stopDebugging`, `vscode.window.onDidWriteTerminalData`, `vscode.env.openExternal`, `vscode.CancellationTokenSource`, `vscode.EventEmitter`, and `vscode.Disposable` — is part of the VS Code public extension API surface, not an independently documentable library.

Porting this component to Tauri/Rust does not require consulting external library documentation. The porting work is entirely about re-mapping VS Code extension API concepts to a Tauri equivalent: the debug-adapter-protocol (DAP) event stream that the tracker currently intercepts would instead be handled by a Rust DAP client (e.g. `dap` crate or a custom async listener), terminal output would come from a Tauri-managed process-output channel, and the "open external URL" action would call Tauri's `shell::open` command. All of those design decisions are architectural choices about the Tauri port as a whole, not questions that require fetching documentation for an external library specific to this extension. There are no npm package docs, third-party SDKs, or external API schemas that are central to understanding or porting this 411-line file.
