(no external research applicable)

The 174 files in `src/vscode-dts/` are pure TypeScript declaration files (`.d.ts`) that define VS Code's public extension API surface using only type/interface declarations; they contain zero `import` statements and zero references to any third-party runtime library, so there is no external library or framework documentation that is central to answering the research question for this partition.
