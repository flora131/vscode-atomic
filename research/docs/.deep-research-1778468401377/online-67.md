(no external research applicable)

The scope under review is a single 29-line file (`extensions/esbuild-webview-common.mts`) described as a thin wrapper that re-exports esbuild build options (format: ESM, platform: browser, target: ES2024) for consumption by seven extension webview bundles. No external documentation is central to porting this file from the TypeScript/Electron context to Tauri/Rust.

The reasoning is as follows. The file's entire surface area consists of standard esbuild `BuildOptions` fields (`format`, `platform`, `target`) that are defined by esbuild's own TypeScript types and require no web lookup to understand or modify. The porting decision — whether to keep esbuild as a JS-layer bundler invoked from a Cargo build script, switch to Vite or Rollup, or fold the webview assets into Tauri's built-in asset system — is an architectural choice about the surrounding build pipeline, not a question answered by the content of this 29-line configuration wrapper. Any Tauri-side documentation about webview asset loading or custom protocol handlers is relevant to the broader migration project but does not change what this specific file does or how its three fields need to be adjusted. Fetching external sources would therefore add no actionable information specific to this file.
