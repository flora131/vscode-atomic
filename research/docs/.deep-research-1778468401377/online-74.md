(no external research applicable)

The `extensions/objective-c/` directory contains only declarative grammar and language-configuration files (two `.tmLanguage.json` TextMate grammars, one `language-configuration.json`, a `package.json` contribution manifest, and a build-time script that calls `vscode-grammar-updater` to pull updated grammars from the upstream `jeff-hykin/better-objcpp-syntax` and `jeff-hykin/better-objc-syntax` repositories) — none of this constitutes runtime IDE logic that requires porting to Tauri/Rust, and the only build-time tooling dependency (`vscode-grammar-updater`) is a well-understood Node utility whose usage is already fully visible in the six-line `build/update-grammars.js` file, so no external documentation lookup is needed to understand or migrate this extension.
