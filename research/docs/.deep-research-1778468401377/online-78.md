(no external research applicable)

The `extensions/sql/` scope is a grammar-only TextMate definition (1 file, 8 LOC) with no runtime dependencies and no external libraries. TextMate grammars are pure declarative JSON/PLIST and are consumed identically by any editor host — including a Tauri-based one — without any porting work. There is nothing Rust- or Tauri-specific to research here.
