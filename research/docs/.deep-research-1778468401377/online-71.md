(no external research applicable)

The scope is limited to `extensions/less/` — a 19-line, grammar/config-only extension with no runtime logic, no Rust/Tauri surface area, and no dependency on any external library whose documentation would bear on a Tauri porting effort.
